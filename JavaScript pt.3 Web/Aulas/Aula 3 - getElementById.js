/*

Selecionando Elementos:

 - getElementById() // Traz apenas um elemento (element)

O HTML é chamado de document. Assim, ao pegar algum elemento do HTML se utiliza o document. 
Quando se pegar um elemento você precisa guardar em algum lugar, 
então o melhor lugar é em uma variável (var, let e const).

*/

const element = document.getElementById("title")

console.log(element)